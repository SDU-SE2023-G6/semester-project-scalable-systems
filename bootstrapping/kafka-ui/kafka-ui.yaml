yamlApplicationConfig:
  kafka:
    clusters:
      - name: yaml
        bootstrapServers:  strimzi-kafka-bootstrap.kafka:9092
  auth:
    type: disabled
  management:
    health:
      ldap:
        enabled: false
